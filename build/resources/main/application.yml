spring:
  application:
    name: ai03
  datasource:
    url: jdbc:postgresql://db.kuovzhfhwgvnncnejmvc.supabase.co:5432/postgres
    username: postgres
    password: a8s52M_uaNXHLLp
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

logging:
  level:
    com.example.ai03: INFO
    org.hibernate.SQL: DEBUG
